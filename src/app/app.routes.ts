import { Routes } from '@angular/router';
import { NutriTrackerComponent } from './nutri/nutriTracker.component';

export const routes: Routes = [
    {path: '', component: NutriTrackerComponent }
];
